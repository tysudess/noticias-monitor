name: Archived — Release V15 Video Open Preview

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical V15 workflow only. Use Release Windows Portable V19 for current builds."
