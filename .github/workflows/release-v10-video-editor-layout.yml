name: Archived — Release V10 Video Editor Layout

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical V10 workflow only. Use Release Windows Portable V19 for current builds."
