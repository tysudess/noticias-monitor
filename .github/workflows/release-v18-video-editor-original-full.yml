name: Archived — Release V18 Video Editor Original Full

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical V18 workflow only. Use Release Windows Portable V19 for current builds."
