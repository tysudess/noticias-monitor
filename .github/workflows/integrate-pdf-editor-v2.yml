name: Archived — Integrate PDF Editor V2

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical workflow only. PDF Editor is frozen and approved; use Release Windows Portable V19 for current builds."
