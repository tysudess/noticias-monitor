name: Archived — Release V17 Qt Preview Engine

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical V17 workflow only. Use Release Windows Portable V19 for current builds."
