name: Archived — Release V11 Functional Video Editor

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical V11 workflow only. Use Release Windows Portable V19 for current builds."
