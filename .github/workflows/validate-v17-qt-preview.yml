name: Archived — Validate V17 Qt Preview

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow
        run: echo "Historical V17 validation only. Use Release Windows Portable V19 for current builds."
